---
# title: "Consultoria & Soluções"
# subtitle: "Análises estatísticas rigorosas para academia e mercado"
page-layout: full
title-block-banner: false
include-in-header:
- text: |
      <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
      <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
      <style>
        .mermaid { margin: 0 auto; text-align: center; }
        .mermaid svg { max-width: 100%; height: auto; }
      </style>
# - text: |
#     <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
#     <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
#- text: |
#      <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
#      <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
#      <style>
#        .mermaid { margin: 0 auto; text-align: center; }
#        /* Garante que o diagrama não fique espremido no celular */
#        .mermaid svg { min-width: 800px; max-width: 100%; height: auto; }
#        .mermaid-scroll { overflow-x: auto; padding-bottom: 10px; }
#      </style>
---

## Análise de Dados

::: {style="text-align: justify; margin-top: 15px; margin-bottom: 40px; font-size: 1.1em; line-height: 1.8;"}

A **ΛΣ Consultoria** oferece suporte técnico especializado para transformar dados complexos em resultados claros, análises estatísticas rigorosas para academia e mercado, atuando na fronteira entre a teoria estatística e a aplicação prática.

- **Academia:** Suporte metodológico para Pesquisas (TCCs, Dissertações e Teses), com foco em reprodutibilidade e rigor científico.

- **Corporativo:** Inteligência de dados, modelagem preditiva e análise de cenários para embasar tomadas de decisão críticas.

:::

## Vamos tirar seu projeto do papel?

```{=html}
<div style="text-align: center; margin-top: 30px; margin-bottom: 30px;">
  <h3 style="font-weight: 900; color: #2a2a2a;">Nossa Metodologia de Trabalho</h3>
  <p style="color: #666;">Transparência do início ao fim do projeto</p>
</div>
```


<div class="mermaid-scroll">
```{mermaid}
%%| fig-align: center
%%| fig-height: 6
flowchart TD
    A["<i class='fa-solid fa-file-pen'></i> 1. Solicitação"] --> B["<i class='fa-brands fa-whatsapp'></i> 2. Contato Ágil"]
    B -->|Diagnóstico| C["<i class='fa-solid fa-handshake'></i> 3. Reunião de Alinhamento"]
    C -->|Escopo Definido| D["<i class='fa-solid fa-file-invoice-dollar'></i> 4. Proposta Comercial"]
    D -->|Aprovação| E["<i class='fa-solid fa-laptop-code'></i> 5. Desenvolvimento"]
    E -->|Finalização| F["<i class='fa-solid fa-rocket'></i> 6. Entrega dos Resultados (Relatórios)"]

    %% FIX: Adicionado rx:10 e ry:10 para cantos arredondados
    classDef start fill:#18bc9c,stroke:#18bc9c,color:#fff,stroke-width:0px,font-weight:bold,rx:10,ry:10; 
    classDef process fill:#fff,stroke:#e0e0e0,color:#333,stroke-width:1px,rx:10,ry:10; 
    classDef endNode fill:#2a2a2a,stroke:#2a2a2a,color:#fff,stroke-width:0px,font-weight:bold,rx:10,ry:10; 

    class A start;
    class B,C,D,E process;
    class F endNode;
    linkStyle default stroke:#bbb,stroke-width:2px;
```

</div>


::: {style="text-align: justify; margin-top: 30px; margin-top: 30px; margin-bottom: 20px; font-size: 1.1em;"}

Preencha o formulário abaixo para uma análise preliminar. Respondemos rapidamente.
:::

```{=html}
<script>
  const form = document.getElementById('contact-form');
  const successMessage = document.getElementById('success-message');
  const userNameDisplay = document.getElementById('user-name-display');
  const submitBtn = document.getElementById('submit-btn');
  const fileInput = document.getElementById('file-upload');
  const fileFeedback = document.getElementById('file-feedback');

  // --- CONFIGURAÇÃO AUTOMÁTICA DE REDIRECIONAMENTO ---
  document.getElementById('next-url').value = window.location.href;

  // 1. Mostrar nomes dos arquivos selecionados
  fileInput.addEventListener('change', function() {
    if (this.files && this.files.length > 0) {
      let fileNames = Array.from(this.files).map(f => f.name).join(', ');
      fileFeedback.innerHTML = `<i class="fa-solid fa-check" style="color:#18bc9c;"></i> <strong>${this.files.length} arquivo(s):</strong> <br><span style="color:#555; font-size:0.9em;">${fileNames}</span>`;
    } else {
      fileFeedback.innerHTML = "";
    }
  });

  // 2. Validação e Envio
  form.addEventListener('submit', function(e) {
    e.preventDefault();
    
    // Validações
    const emailField = document.getElementById('input-email');
    if (!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(emailField.value)) {
        alert("Por favor, insira um e-mail válido.");
        emailField.focus(); return;
    }

    const whatsField = document.getElementById('input-whatsapp');
    const whatsValue = whatsField.value.replace(/\D/g, ''); 
    if (whatsValue.length > 0 && (whatsValue.length < 10 || whatsValue.length > 11)) {
        alert("O número de WhatsApp parece incompleto. Use DDD + Número.");
        whatsField.focus(); return;
    }
    
    // Preparar Envio
    const originalBtnText = submitBtn.innerHTML;
    submitBtn.innerHTML = '<i class="fa-solid fa-spinner fa-spin"></i> Enviando...';
    submitBtn.style.opacity = "0.7";
    submitBtn.disabled = true;

    // Obtém o primeiro nome para a mensagem de sucesso
    const nome = document.getElementById('input-nome').value.split(' ')[0];
    
    // 🔴 NOVO FIX: Trata envio com arquivos. Exibe a mensagem de sucesso E força o envio tradicional
    if (fileInput.files && fileInput.files.length > 0) {
        
        // 1. Exibe a mensagem de sucesso (simulação) IMEDIATAMENTE antes do envio
        userNameDisplay.innerText = nome;
        $(form).fadeOut(500, function() { $(successMessage).fadeIn(500); });
        $('html, body').animate({ scrollTop: $(".form-container").offset().top - 100 }, 800);
        
        // 2. Garante o envio tradicional (Plano B)
        console.warn("Arquivos detectados. Forçando exibição de sucesso e envio tradicional.");
        
        // Remove listener e define a ação para o modo tradicional (sem /ajax)
        form.removeEventListener('submit', arguments.callee);
        form.action = "https://formsubmit.co/alexssandrosg@gmail.com"; 
        
        // 3. Aguarda um momento para a animação de fadeOut e envia o formulário no modo tradicional
        setTimeout(() => {
            form.submit();
        }, 1000); // Dá 1 segundo para o usuário ver a mensagem antes de recarregar
        
        return; 
    }

    // --- Se NÃO houver arquivos, continua com o AJAX (Envio Silencioso) ---
    const formData = new FormData(form);

    fetch("https://formsubmit.co/ajax/alexssandrosg@gmail.com", {
      method: "POST",
      headers: { 'Accept': 'application/json' },
      body: formData
    })
    .then(response => {
        if (!response.ok) { throw new Error('Falha AJAX'); }
        return response.json();
    })
    .then(data => {
      // Sucesso AJAX (Aparece a mensagem verde sem recarregar)
      console.log("Sucesso:", data);
      userNameDisplay.innerText = nome;
      $(form).fadeOut(500, function() { $(successMessage).fadeIn(500); });
      $('html, body').animate({ scrollTop: $(".form-container").offset().top - 100 }, 800);
    })
    .catch(error => {
      // Falha AJAX (Plano B Genérico, se falhar mesmo sem arquivo)
      console.warn("Erro AJAX, tentando fallback:", error);
      
      form.removeEventListener('submit', arguments.callee);
      form.action = "https://formsubmit.co/alexssandrosg@gmail.com";
      form.submit();
    });
  });
</script>
```

